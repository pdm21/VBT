{
  "name": "vbt-server",
  "version": "1.0.0",
  "description": "VBT System Server",
  "main": "server.js",
  "engines": {
    "node": "20.18.3",
    "npm": "10.8.2"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup": "npm install && cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start:prod": "npm run build && npm start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "next": "^14.0.1",
    "serialport": "^12.0.0",
    "@serialport/parser-readline": "^12.0.0",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}

